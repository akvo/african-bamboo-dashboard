export const PREFIX_PLOT_ID = "PLT";
export const PREFIX_FARM_ID = "AB";

/** Question types excluded from table columns and sortable fields. */
export const EXCLUDED_QUESTION_TYPES = ["geoshape", "geotrace", "geopoint"];

/** Question names excluded from table dynamic columns. */
export const EXCLUDED_QUESTION_NAMES = ["region", "region_specify"];
