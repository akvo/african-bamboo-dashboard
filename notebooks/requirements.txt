# Dependencies for the V24-V27 geospatial POC notebooks.
#
# These are notebook-only. Nothing here belongs in backend/requirements.txt;
# the production feature ships Rasterio/GDAL through the worker image instead.
#
# Install into whatever environment runs the notebooks:
#     pip install -r notebooks/requirements.txt
#
# Versions are the ones the POC results in notebooks/outputs/ were produced
# with. The notebooks also need the backend on sys.path, which poc_common
# arranges, but they do not need Django installed or a database.

numpy==2.0.0
rasterio==1.3.10
shapely==2.0.4
pyproj==3.6.1
requests==2.31.0

# Inline map preview of the synthetic flagged-plot examples.
folium==0.20.0
